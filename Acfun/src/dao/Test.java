package dao;

import java.math.BigInteger;

import database.ISHCDataOperator;

public class Test {
	public static void main(String[] args) {/*
		ISHCDataOperator op = new ISHCDataOperator();
		VideoDAO.configure("http://www.bilibili.tv/", "001", "2");
		
		VideoDAO video = new VideoDAO();
		video.ID = new BigInteger("0010200000000000000");
		video.author = new BigInteger("0");
		try {
			op.insert(video);
			op.emptyCache();
		} catch (Exception e) {
			e.printStackTrace();
		} finally {
			op.printTerminatePoint();
		}
		op.disconnect();*/
	}
}
